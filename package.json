{
  "name": "truescore",
  "private": true,
  "scripts": {
    "build": "bun build.ts",
    "watch": "bun build.ts --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
